Run via `python -m sync.metadata_sync` (requires `DORIS_HOST/PORT/USER/PASSWORD` env vars and a configured datasource in `adh_datasources`); `python -m sync.rebuild_vectors` rebuilds all embeddings; `python encrypt_passwords.py` migrates plaintext passwords once; `python -m sync.seed_mcp_market` seeds the MCP registry; schema is initialized by executing `create_adh_tables.sql` against Doris.