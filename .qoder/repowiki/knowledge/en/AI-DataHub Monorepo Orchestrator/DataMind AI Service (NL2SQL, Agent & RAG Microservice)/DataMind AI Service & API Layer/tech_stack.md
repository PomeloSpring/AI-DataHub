FastAPI routers with Pydantic request models; Server-Sent Events for streaming NL2SQL and agent pipelines; MySQL metadata DB via `services.shared.common.db.metadata_db`; vector store via `services.shared.common.vector` (upsert into `adh_knowledge_chunks`); Neo4j knowledge graph via `services.datamind.rag.graph_rag.neo4j_store` for graph-augmented RAG; LLM calls through `services.shared.common.llm.llm_client`.