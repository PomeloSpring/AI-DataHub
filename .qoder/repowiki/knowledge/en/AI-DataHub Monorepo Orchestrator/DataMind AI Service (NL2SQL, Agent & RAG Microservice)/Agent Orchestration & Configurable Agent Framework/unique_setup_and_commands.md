Agents are discovered at runtime by scanning `config/agents/` directories — adding a new agent means creating a folder with `skill.yaml` and `system.md`, then registering its instance via `register_agent()`. Route patterns can be hot-reloaded through `reload_route_patterns()` after admin updates. Per-request overrides of `max_iterations` are supported by passing the argument into `ConfigurableAgent.run()`.