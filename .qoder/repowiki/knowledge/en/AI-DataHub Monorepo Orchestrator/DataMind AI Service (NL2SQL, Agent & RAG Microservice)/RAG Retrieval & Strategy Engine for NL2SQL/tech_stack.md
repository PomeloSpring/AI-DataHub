Doris OLAP with HNSW vector index (`l2_distance_approximate`) as primary vector store, with a `VectorStore` abstraction falling back to SQLite; pure-Python Okapi BM25 for sparse retrieval; Reciprocal Rank Fusion (RRF) for ranking fusion; NetworkX for in-memory graph traversal; optional Neo4j via `graph_rag/` for persistent knowledge graphs; `pymysql` for direct metadata DB access.