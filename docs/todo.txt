1:在右上角的切换模块左侧添加一个AS-BOT按钮，点开后右侧自动加载一个纵向窗口，里面是项目自身的ai助手，可以帮助用户使用本系统，内部集成一个系统级waker，这个按钮只有指定角色的人可用，这个系统级waker绑定项目内置知识库和当前服务自身能力的本体模型，不在数据中台管理，在系统配置中开一个AI助手的菜单项，里面内置本项目的能力。协助用户构建本体、构建元数据，但是同样不允许直连数据源，所有信息都必须依靠api的tool获取，这些能力以本体模型的形式存放，绑定action动作和权限。
2:当前项目缺少可观测的部分，尤其是llm交互过程，需要在管理端查看session清单、用户用量、模型用量，具体对话执行的trace，以及不在chat中输出的各个tools或者mcp的执行产物，比如语义层生成的sql、llm的nl2sql的产出sql，权限转化前后的sql，sql执行的输出等等，要有完整的可观测链路
3：我认为应该是先用知识库理解业务，然后判断怎么调用语义层，至于语义层有哪些内容，我觉得是否也可以放在知识库中做rag检索，这样可以减少llm判断的过程，另外系统内置的skills都是一些查询维度的，我认为应该按照现在的交互方式进行整和到一个skills中，另外本体模型要支持绑定知识库，如果更新后自动更新对于的知识库，方便llm使用知识库时自动了解到最新的本体模型，然后直接调用，减少不必要的多余交互。
4：对于有联邦查询的需求，使用doris数据源，语义建模时要能检索到multicatalog
5：帮忙把阿里云sls的语法的官方skills导入
6：增加IM能力，支持DMS接入钉钉聊天机器人
7：增加一个工单的功能，chat中如果某个数据或者分析处理不了，可以帮用户提交工单给指定的角色的用户，指定角色用户处理完后可以反馈结果，这里在右上角加一个消息通知图标的按钮，点击可以查看提交的工单和执行情况，以及系统和用户的反馈消息
8：把项目的安全围栏要求写到.qoder/rules，保障后续迭代过程中务必遵循这个规范

9：很多时候我们只靠原始数据无法分析数据，还需要清洗、转换，所以在llm交互中有时需要同步数据，这个时候llm在用户允许的情况下可以调用同步任务，在现有的数据源中进行数据流转，整个过程不能暴露数据源敏感信息，同步的数据也要依照权限限制，不能查看的字段不能同步，需要脱敏的数据必须脱敏等等。